// basically it's off we may have to work on dashboards since it have all apossible action on the dashboard
import * as Types from "./actionTypes";
import dashboardApi from "../api/dashboardApi";
import { netError } from "./net";
export function loadDashboards(dashboard: any, user: any) {}
